Exercise 2 --> If you run mvn test it will be skip all the tests created in src/test/java
Exercise 3 and 4 --> I put the generated files (jar and war) 

Also I added to .gitignore the folders of the project I wanted to ignore (like \target p.e)